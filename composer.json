{
    "name": "musicglue/sidekiq-job-pusher",
    "description": "PHP client to interface with Sidekiq",
    "require": {
        "php": ">=5.3.3",
	"predis/predis": "^1.0"
    },
    "autoload": {
        "psr-0": {
            "": "lib/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "3.7.*"
    },
    "config": {
        "bin-dir": "bin"
    }
}
